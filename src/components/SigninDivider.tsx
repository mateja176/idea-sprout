import Box from '@material-ui/core/Box';
import Typography from '@material-ui/core/Typography';
import React from 'react';

export const SigninDivider: React.FC = () => (
  <Box mt={3} mb={2}>
    <Typography>Or sign in with</Typography>
  </Box>
);
